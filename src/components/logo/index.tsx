'use client';
import { Typography } from '@mui/material';
import { FC } from 'react';
interface LogoProps {}

const Logo: FC<LogoProps> = () => {
  return (
    <Typography
    
     className="text-white"
    >
      LOGO
    </Typography>
  );
};

export default Logo;
